[metadata]
id = "invoice-extraction"
version = "1.0.0"

[tool.fastskill.eval]
prompts = "evals/prompts.csv"
checks = "evals/checks.toml"
trials_per_case = 1
pass_threshold = 1.0
fail_on_missing_agent = true
